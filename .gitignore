node_modules/
data/
*.db
*.db-shm
*.db-wal
.env
.DS_Store
npm-debug.log*
coverage/

# Binary reference material — kept locally, not in the repo. The Vistage
# API PDF in particular carries integration details that must not be
# committed to version history.
*.docx
*.pdf
*.xlsx

# Office / editor lock & temp files
~$*
~WRL*.tmp
*.tmp

# Demo / sample contact data — kept locally for manual testing, never
# committed (may contain contact-shaped data that does not belong in
# version history).
*.csv
